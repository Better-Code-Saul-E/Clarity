<Window x:Class="Clarity.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clarity.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Border DockPanel.Dock="Top" Height="50" Background="#FF333333">
            <Grid>
                <TextBlock Text="Clarity" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           VerticalAlignment="Center" 
                           Margin="20,0,0,0"/>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Left" Width="200" Background="#FFFAFAFA" BorderBrush="#FFEEEEEE" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <Button Content="Boards (TBA)" Margin="5"/>
                <Button Content="Calendar(TBA)" Margin="5"/>
                <Button Content="Settings (TBA)" Margin="5"/>
            </StackPanel>
        </Border>

        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">

            <ItemsControl ItemsSource="{Binding Columns}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="300" Margin="10" Background="#FFEAEAEA" CornerRadius="5">

                            <StackPanel>

                                <TextBlock Text="{Binding Title}" 
                                           FontSize="18" 
                                           FontWeight="Bold" 
                                           Margin="10"/>

                                <ListBox ItemsSource="{Binding Tasks}" 
                                         BorderThickness="0" 
                                         Background="Transparent">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>

                                            <Border Margin="5" 
                                                    Padding="10" 
                                                    Background="White" 
                                                    CornerRadius="3" 
                                                    BorderBrush="#FFDDDDDD" 
                                                    BorderThickness="1">
                                                <TextBlock Text="{Binding Title}"/>
                                            </Border>

                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </DockPanel>
</Window>
